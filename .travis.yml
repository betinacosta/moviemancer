language: python
python: 3.6.5

install:
  - pip install pipenv
  - pipenv install
  - python -m pip install coveralls

jobs:
  include:
    - stage: unit
      script: coverage run --source='.' manage.py test moviemancer

after_success:
  - coveralls